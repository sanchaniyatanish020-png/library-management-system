[phases.setup]
nixPkgs = ["php83", "php83Extensions.mongodb", "php83Extensions.zip", "composer"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-scripts --no-interaction"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8000} -t public"